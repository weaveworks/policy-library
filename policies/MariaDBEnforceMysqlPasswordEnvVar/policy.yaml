apiVersion: pac.weave.works/v1
kind: Policy
metadata:
  name: weave.policies.mariadb-enforce-mysql-password-env-var
spec:
  id: weave.policies.mariadb-enforce-mysql-password-env-var
  name: MariaDB Enforce Environment Variable - MYSQL_PASSWORD
  description: |
    This Policy ensures MYSQL_PASSWORD environment variable are in place when using the official container images from Docker Hub.
    MYSQL_PASSWORD: The MYSQL_PASSWORD environment variable specifies a password for MARIADB_USER user.
  how_to_solve: |
    If you encounter a violation, ensure the MYSQL_PASSWORD environment variables is set.
    For futher information about the MariaDB Docker container, check here: https://hub.docker.com/_/mariadb
  category: weave.categories.access-control
  severity: high
  controls:
  - weave.controls.pci-dss.2.1
  - weave.controls.hipaa.164.312.a.1
  - weave.controls.hipaa.164.312.a.2.i
  - weave.controls.gdpr.24
  - weave.controls.gdpr.25
  - weave.controls.gdpr.32
  targets: {kinds: [Deployment, Job, ReplicationController, ReplicaSet, DaemonSet,
    StatefulSet, CronJob]}
  tags: [
    "pci-dss",
    "mitre-attack",
    "hipaa",
    "gdpr",
  ]
  parameters:
  - value: 
    name: exclude_namespace
    required: false
    type: string
  - value: 
    name: exclude_label_key
    required: false
    type: string
  - value: 
    name: exclude_label_value
    required: false
    type: string


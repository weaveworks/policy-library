apiVersion: pac.weave.works/v2beta1
kind: Policy
metadata:
  name: weave.templates.container-maximum-memory-limits
spec:
  id: weave.templates.container-maximum-memory-limits
  name: Container Maximum Memory Limits
  description: |
    This Policy is to ensure a maximum Memory Limit value is set. The policy will fail if the set Memory limit is higher than the one specified in the policy. You must specify a unit size (e.g Mi, Gi, etc).
  how_to_solve: |
    ```
    ...
      spec:
        containers:
        - resources:
            limits:
              memory: <size>
    ```

    https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
  category: weave.categories.capacity-management
  severity: medium
  standards:
  - id: weave.standards.soc2-type-i
    controls:
    - weave.controls.soc2-type-i.2.1.1
  parameters:
  - name: size
    type: string
    required: true
    value:
  - name: exclude_namespace
    type: string
    required: false
    value:
  - name: exclude_label_key
    type: string
    required: false
    value:
  - name: exclude_label_value
    type: string
    required: false
    value:
